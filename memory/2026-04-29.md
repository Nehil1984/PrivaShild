- PrivaShield Security- und Härtungstag fortgeführt und erfolgreich auf Version 1.8.0 abgeschlossen.
- Mandanten- und Gruppentrennung gehärtet: `GET /api/mandanten-gruppen` liefert Nicht-Admins jetzt nur noch Gruppen, die über ihre freigegebenen Mandanten erreichbar sind; zusätzliche Tests gegen Gruppen-Leaks ergänzt.
- Backup/Restore gehärtet: Restore-Preflight/Dry-Run vor Wiederherstellung ergänzt, Backend-/Migrationsprüfung eingebaut, offensichtliche Backup-Defekte werden vor produktivem Restore abgefangen.
- Produktionsstart gehärtet: Server bricht nun in `production` bei schwachem oder Platzhalter-`JWT_SECRET` bzw. schwachem Platzhalter-Admin-Passwort ab.
- Docker-Härtung erweitert: Compose nutzt keine festen Demo-Secrets mehr, Healthcheck korrigiert sowie `no-new-privileges`, `cap_drop: ALL` und `tmpfs` ergänzt.
- Backup-Konfiguration lokal auf Verschlüsselung umgestellt (`encrypt: true`), damit künftige Sicherungen standardmäßig verschlüsselt laufen, sofern Laufzeitkennwort gesetzt ist.
- Verifikation erfolgreich: `npm test`, `npm run check` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün. Teststand danach: 12 Dateien / 44 Tests erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `db3f8fe` (`feat: harden tenant isolation backup restore and production startup`). Zusätzlicher Branch `1.8.0` erstellt und gepusht.
- Hotfix-Release auf `1.8.1` erstellt, nachdem der Production-Startup-Guard in `1.8.0` Bestandsinstallationen fälschlich wegen `INITIAL_ADMIN_PASSWORD` blockierte.
- Fix: `JWT_SECRET` bleibt harter Production-Guard; die Passwortstärke von `INITIAL_ADMIN_PASSWORD` wird jetzt nur noch beim tatsächlichen Initial-Seeding des ersten Admin-Benutzers erzwungen.
- Verifikation für `1.8.1` erfolgreich: `npm test`, `npm run check` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün. Teststand blieb bei 12 Dateien / 44 Tests erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `94b35b5` (`fix: relax production startup guard for existing installs`). Zusätzlicher Branch `1.8.1` erstellt und gepusht.
- Auth-Härtung weitergeführt und Release `1.8.2` erstellt.
- Backend setzt jetzt beim Login zusätzlich ein `HttpOnly`-Auth-Cookie (`privashield_auth`), bietet einen Logout-Endpunkt zum serverseitigen Cookie-Löschen und akzeptiert in der Auth-Middleware Cookie oder Bearer-Token für eine migrationsfreundliche Übergangsphase.
- Frontend von primärem `localStorage`-Tokenfluss entkoppelt: Session-Wiederherstellung läuft jetzt über `/api/auth/me` mit `credentials: same-origin`; Logout ruft den Server-Endpunkt auf. `localStorage` bleibt nur noch für UI-Kontext wie aktiven Mandanten, nicht mehr als primärer Auth-Träger.
- Verifikation für `1.8.2` erfolgreich: `npm test`, `npm run check` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün. Teststand blieb bei 12 Dateien / 44 Tests erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `96a9fb8` (`feat: move auth flow to httpOnly cookie session`). Zusätzlicher Branch `1.8.2` erstellt und gepusht.
- Hotfix-Release `1.8.3` erstellt, nachdem der neue Cookie-Login-Flow in der Praxis einen Login-Hänger ausgelöst hatte.
- Ursache: Der Login-Request selbst setzte `credentials: same-origin` nicht explizit, wodurch das Auth-Cookie je nach Laufumgebung nicht zuverlässig übernommen wurde. Folge: Session-Wiederherstellung/Mandantenladen lief nach erfolgreichem Login ins Leere.
- Fix: `fetch('/api/auth/login')` explizit mit `credentials: 'same-origin'` ergänzt. Zusätzlich blieb die zuvor ergänzte Fehlerbehandlung für fehlgeschlagenen Mandantenkontext erhalten.
- Verifikation für `1.8.3` erfolgreich: `npm test`, `npm run check` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün. Teststand weiterhin 12 Dateien / 44 Tests erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `bd1c951` (`fix: include credentials in cookie login flow`). Zusätzlicher Branch `1.8.3` erstellt und gepusht.

- PrivaShield Auth-/Security-Block am 2026-04-29 weitergeführt: nach 1.8.1 wurde zunächst 1.8.2 umgesetzt mit Auth-Umbau weg vom primären localStorage-Token hin zu HttpOnly-Cookie-Session-Kompatibilität. Backend setzt seitdem `privashield_auth` als HttpOnly-Cookie beim Login, bietet Logout-Endpunkt zum serverseitigen Löschen und akzeptiert in der Auth-Middleware Cookie oder Bearer für die Übergangsphase. Frontend stellt Sitzung über `/api/auth/me` mit `credentials: "same-origin"` wieder her und nutzt `localStorage` nicht mehr für den Auth-Token, nur noch für UI-Kontext wie aktiven Mandanten.
- Release 1.8.2 wurde verifiziert mit `npm test`, `npm run check` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build`, danach nach `master` gepusht und Branch `1.8.2` angelegt. Commit: `96a9fb8` (`feat: move auth flow to httpOnly cookie session`).
- Direkt danach Regress im Live-Betrieb entdeckt: nach Login hing die App im Mandantenkontext bzw. zeigte Auth-Fehler. Erster Hotfix 1.8.3: Login-Fetch erhielt explizit `credentials: "same-origin"`, damit das Auth-Cookie nach dem Login zuverlässig übernommen wird; zusätzlich wurde der endlose Ladezustand im Mandantenkontext abgefangen. Auch 1.8.3 wurde erfolgreich mit Test/Check/Build verifiziert, nach `master` gepusht und Branch `1.8.3` angelegt. Commit: `bd1c951` (`fix: include credentials in cookie login flow`).
- Trotz 1.8.3 zeigte der Live-Test weiter `401 Nicht authentifiziert` bei `/api/auth/me` und `/api/mandanten`, obwohl `login` 200 war. User lieferte Browser-Network-Beweise: `Set-Cookie` für `privashield_auth` war vorhanden, Cookie wurde im Browser gespeichert (HttpOnly, Secure, SameSite=Strict). Daraus wurde abgeleitet, dass sehr wahrscheinlich das serverseitige Cookie-Parsing im Backend fehlerhaft bzw. nicht robust genug für reale Header mit vielen Cookies ist.
- Anschließend wurde ein weiterer Backend-Fix begonnen: robustes serverseitiges Cookie-Parsing statt fragiler Header-Auswertung. Technische Verifikation (mindestens Build; laut Zwischenstand auch Test/Check) lief grün. Offener Punkt nach Memory-Flush: User muss den Login nach diesem Parsing-Fix erneut live testen; danach voraussichtlich nächster Hotfix-Release, falls bestätigt.
- Zusätzlichen Hotfix `1.8.4` veröffentlicht, um das serverseitige Auslesen des Auth-Cookies robuster zu machen. Hintergrund: Im Live-Test wurden `401 Nicht authentifiziert` bei `/api/auth/me` und `/api/mandanten` beobachtet, obwohl `login` 200 war und der Browser das `privashield_auth`-Cookie gespeichert hatte.
- Fix in `server/routes.ts`: Cookie-Parsing für `privashield_auth` von fragiler Gesamtheader-Auswertung auf segmentweises Parsen der einzelnen Cookies umgestellt, damit reale Cookie-Header robuster verarbeitet werden.
- Version auf `1.8.4` erhöht. Commit `fc43cb3` mit Message `fix: harden auth cookie parsing` nach `master` gepusht; zusätzlicher Branch `1.8.4` erstellt und gepusht.
- Weiterer Login-Hotfix `1.8.5` veröffentlicht, nachdem Screenshots zeigten: Login antwortet 200 mit `Set-Cookie: privashield_auth=...`, aber Folge-Requests (`/api/auth/me`, `/api/mandanten`) senden im echten Deployment stattdessen nur ein bestehendes `token`-Cookie bzw. proxy-/hostseitig umgeschriebene Cookies. Das deutet auf reales Proxy-/Host-Verhalten hin, das den neuen Cookie-Namen nicht verlässlich durchreicht.
- Fix in `server/routes.ts`: Auth liest jetzt nicht nur `privashield_auth`, sondern akzeptiert zusätzlich wieder das bestehende `token`-Cookie als Fallback (`readNamedCookie(..., 'token')`). Damit bleibt der Login auch unter dem beobachteten Unraid-/Proxy-Cookie-Verhalten funktionsfähig.
- Version auf `1.8.5` erhöht. Voll verifiziert mit `npm test`, `npm run check` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build`. Commit `19f443b` (`fix: accept legacy token cookie for auth`) nach `master` gepusht; zusätzlicher Branch `1.8.5` erstellt und gepusht.
- Nachdem auch 1.8.5 den Login im realen Deployment nicht reparierte, zeigten weitere Network-Screenshots: Browser speichert sowohl `privashield_auth` als auch `token`, aber Folge-Requests (`/api/auth/me`, `/api/mandanten`) liefern weiter 401. Auffällig war, dass im Request-Header zwar `token=` mitgesendet wird, Cookie-basierte Auth aber im echten Host-/Proxy-Setup unzuverlässig bleibt.
- Pragmatischer Kompatibilitätsfix in `1.8.6`: Frontend nutzt den vom Login gelieferten JWT wieder zusätzlich als Bearer-Fallback. `client/src/lib/queryClient.ts` verwaltet erneut `authToken` + `setApiAuthToken(...)`; `AuthProvider` in `client/src/App.tsx` speichert `privashield_token` wieder in `localStorage`, setzt den Token für API-Requests und ruft `/api/auth/me` explizit mit `Authorization: Bearer <token>` auf. Cookie-Session bleibt zusätzlich aktiv, aber der Login hängt nicht mehr ausschließlich an Cookie-Verhalten des Host-/Proxy-Setups.
- Version auf `1.8.6` erhöht. Voll verifiziert mit `npm test`, `npm run check` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build`. Commit `ffbd282` (`fix: restore bearer auth fallback for login flow`) nach `master` gepusht; zusätzlicher Branch `1.8.6` erstellt und gepusht.
- Fachliche Erweiterung des VVT-/Verarbeitungen-Moduls veröffentlicht: Verarbeitungstätigkeiten besitzen jetzt eine strukturierte Risikobewertung mit `risikostufe`, `risikobegruendung`, `risikoTriggers` und `risikopruefungAm`.
- Die Logik leitet die Vorbewertung aus fachlichen Risikotreibern ab (u. a. Drittlandtransfer, DSFA-Pflicht, besondere Kategorien personenbezogener Daten, schutzbedürftige betroffene Gruppen, Profiling/Überwachung/KI/Automatisierung sowie externer Empfängerkreis) und verzahnt VVT dadurch enger mit DSFA- und Governance-Logik.
- UI-Erweiterung in `client/src/App.tsx`: VVT-Formular zeigt jetzt Risikostufe, Trigger, Begründung und Prüfdatum; Listenansicht zeigt Risiko-Badge, fachliche Begründung und DSFA-Bezug. Speicherverdrahtung für LowDB nachgezogen.
- Version auf `1.9.0` erhöht. Verifikation erfolgreich mit `npm run check` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build`. Commit `bb7274a` (`feat: add linked risk assessment to processing records`) nach `master` gepusht; zusätzlicher Branch `1.9.0` erstellt und gepusht.
- Ausbau des neuen VVT-Risikoblocks weitergeführt und als `1.10.0` veröffentlicht.
- Auf der VVT-Seite wurden aus der Risikobewertung operative Folgepfade ausgebaut: zusätzliche Filter/Fokusfälle für `high-risk` und `review-needed`, direkte Aufgabenentwürfe, One-click-Erzeugung von Folgeaufgaben mit Dublettenschutz sowie direkte Erzeugung verknüpfter PDCA-Zyklen inkl. Folgeaufgabe für Hochrisiko-, DSFA-, Drittland-, Review- und Löschkonzept-Fälle.
- Dashboard-/Governance-Logik berücksichtigt nun VVT mit hoher Risikostufe und VVT mit Reviewbedarf als priorisierte Hinweise.
- DSFA-Anlage wurde fachlich enger mit VVT verzahnt: Beim VVT-Prefill werden jetzt auch `risikostufe`, `risikobegruendung`, `risikoTriggers`, `drittlandtransfer`, TOM-Hinweise und Prüfdatum in die DSFA-Risikostruktur und Begründungsfelder übernommen, einschließlich sinnvoller Vorbelegung des ersten Risikoblocks sowie Art.-36-/Review-Nähe bei Hochrisikofällen.
- Version auf `1.10.0` erhöht. Commit `744968c` (`feat: connect processing risk follow-ups with dsfa and pdca`) nach `master` gepusht; zusätzlicher Branch `1.10.0` erstellt und gepusht.
