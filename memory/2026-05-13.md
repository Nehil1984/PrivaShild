# 2026-05-13

- VulnLedger GitHub-Stand geprüft: `master` und Branch `0.4.1` zeigten bereits auf Commit `9db98f4` (`feat: remove demo seed data and hardcoded defaults`). Die Version `0.4.1` war also bereits auf GitHub vorhanden; die Abweichung in der Wahrnehmung lag sehr wahrscheinlich an einer GitHub-Ansicht auf einem älteren Branch oder an gecachter Anzeige.
- Anschließend Repo-Härtung gegen versehentliches Einchecken lokaler Laufzeitartefakte umgesetzt:
  - `.gitignore` in `app/VulnLedger/vuln-ledger` um `data/` und `server-dist/` ergänzt.
  - Zweck: lokale Datenbank-/Backup-/Config-Dateien sowie Server-Buildartefakte bleiben zuverlässig lokal und geraten nicht versehentlich in künftige Commits.
- Verifikation erfolgreich mit:
  - `npm run build:full`
  - `npm run lint`
- Git:
  - Commit: `9bd4ad9`
  - Message: `chore: ignore local runtime artifacts`
  - Push auf `master` erfolgreich
  - Branch `0.4.1` erneut auf denselben neuen Commit aktualisiert/gepusht
- Ergebnis: GitHub enthält jetzt nicht nur VulnLedger `0.4.1`, sondern zusätzlich auch die Repo-Härtung, damit lokale Runtime-Dateien künftig nicht versehentlich mit versioniert werden.

- VulnLedger-Nutzbarkeitsanpassung weitergeführt:
  - UI-Version in `src/App.tsx` auf den tatsächlichen Stand `0.4.1` korrigiert (zuvor stand dort fälschlich noch `0.4.0`).
  - Mandanten-/Konzernansicht auf der Seite `Mandanten` nutzbarer gemacht: Statt nur einfacher Listen gibt es jetzt selektierbare Übersichten mit direkt sichtbaren Detailinformationen zu Konzernen, zugeordneten Mandanten, Ansprechpartnern, Branche, Notizen und Anzahl zugehöriger Assessments.
  - Ziel: Stammdaten lassen sich schneller erfassen und prüfen, ohne dass man sich nur durch reine Rohlisten arbeiten muss.
- Verifikation erfolgreich mit:
  - `npm run build:full`
  - `npm run lint`
- Git:
  - Commit: `c116a94`
  - Message: `feat: improve tenant overview usability`
  - Push auf `master` erfolgreich
  - Branch `0.4.1` auf denselben Commit aktualisiert

- Weitere große Nutzbarkeitsrunde für VulnLedger umgesetzt:
  - **Mandanten & Konzerne:** echte Bearbeiten-Funktion ergänzt. Ausgewählte Konzerne und Mandanten lassen sich jetzt direkt im UI aktualisieren; die vorhandenen `PUT`-APIs für `/api/groups/:id` und `/api/customers/:id` werden nun auch im Frontend genutzt.
  - **Assessments:** freie Suche plus Statusfilter ergänzt, damit größere Projektlisten schneller eingegrenzt werden können.
  - **Findings:** freie Suche sowie Filter nach Severity und Status ergänzt; zusätzlich wurde die Detailansicht um das Target klarer ergänzt.
  - **Reports:** Report-Erstellung fachlich erweitert um zusätzliche Kontextfelder für `Methodik / Testansatz`, `Risiko-Zusammenfassung` und `Fazit / nächste Schritte`. Diese Inhalte werden beim Speichern strukturiert in die Zusammenfassung übernommen und sind vorab im Report-Kontext sichtbar.
  - **UI/CSS:** zusätzliche Filterleisten und responsive Darstellung dafür ergänzt.
- Verifikation erfolgreich mit:
  - `npm run build:full`
  - `npm run lint`
- Git:
  - Commit: `0190ee1`
  - Message: `feat: improve tenant, finding, and report workflows`
  - Push auf `master` erfolgreich
  - Branch `0.4.1` auf denselben Commit aktualisiert

- Nächste große Ausbauphase für VulnLedger abgeschlossen:
  - **Assessments:** vollständige Bearbeiten-Funktion ergänzt sowie Löschen/Archivieren im UI und Backend verdrahtet; abhängige Daten wie Findings, Evidence, Retests und assessmentbezogene Reports werden beim Löschen konsistent mitbereinigt.
  - **Findings:** vollständige Bearbeiten-Funktion ergänzt sowie Löschen/Archivieren im UI und Backend ergänzt; verknüpfte Evidence- und Retest-Einträge werden ebenfalls sauber entfernt.
  - **Reports:** vollständige Bearbeiten-Funktion ergänzt sowie Löschen/Archivieren umgesetzt; die bisher nur bei der Neuerstellung gepflegten Kontextfelder für Executive Summary, Methodik, Risiko und Fazit sind jetzt auch beim Bearbeiten strukturiert pflegbar.
  - **Report-Export:** Druck-/Exportansicht in `report-print.html` deutlich lesbarer gemacht, indem Executive Summary, Methodik, Risiko-Zusammenfassung und Fazit als eigene Blöcke dargestellt werden; Findings werden strukturierter mit Target, CVSS/CWE, Empfehlung, Evidence und Retest-Inhalten ausgegeben.
  - **Backend/API:** neue `PUT`- und `DELETE`-Routen für Assessments, Findings und Reports sowie passende Update-/Delete-Funktionen in LowDB- und SQLite-Storage umgesetzt.
- Verifikation erfolgreich mit:
  - `npm run build:full`
  - `npm run lint`
- Git:
  - Commit: `58e9a26`
  - Message: `feat: add edit, archive, and report export workflows`
  - Push auf `master` erfolgreich
  - Branch `0.4.1` per `--force-with-lease` auf denselben Commit aktualisiert, weil der lokale Branchstand hinter `origin/0.4.1` lag

- Versionspflege für VulnLedger direkt nachgezogen:
  - `package.json` von `0.4.1` auf `0.4.2` erhöht.
  - Angezeigte UI-Version in `src/App.tsx` ebenfalls auf `0.4.2` aktualisiert.
  - Verifikation erneut erfolgreich mit `npm run build:full` und `npm run lint`.
  - Git-Commit: `c9e5306`
  - Message: `chore: bump version to 0.4.2`
  - Push auf `master` erfolgreich.
  - Neuer Versions-Branch `0.4.2` erstellt und gepusht.
