# 2026-04-20

- PrivaShield weitergeführt und als nächsten sinnvollen Schritt den fachlichen Abschluss rund um Löschkonzept + Export priorisiert.
- Löschkonzept-Codepfad geprüft: Formular, Schema, API, Storage und Export-Kontext sind technisch verdrahtet; kein offensichtlicher harter Save-Blocker im Code gefunden.
- Export für `loeschkonzept` erweitert, sowohl in `client/src/export-page.tsx` als auch in `client/print.html`.
- Druck-/Exportfelder für Löschkonzept erweitert um gesetzliche Frist/Referenz, Aufbewahrungsfrist, Löschereignis, Rechtsgrundlage, Löschverantwortlicher, Systeme/Speicherorte, Kontrolle, Nachweis und Status.
- Löschkonzept-Summary im Druckexport verbessert, u. a. für Fristbezug, Löschereignis, Nachweis und kritische LK5-Fälle.
- Build (`npm run build`) und Typecheck (`npm run check`) erfolgreich durchgelaufen.
- Version von `1.0.0` auf `1.1.0` erhöht.
- Commits erstellt:
  - `2d0105a` `feat: erweitere loeschkonzept-export`
  - `1052acc` `chore: bump version to 1.1.0`
- Änderungen erfolgreich nach `master` gepusht.
- Zusätzlich einen GitHub-Branch `1.1.0` erstellt.
- Funktionstest erfolgreich gegen den laufenden lokalen Server ausgeführt: Login, Erstellen, Bearbeiten, Persistenzprüfung, Export-Kontext-Prüfung und Löschen eines Löschkonzept-Testeintrags.
- Dabei bestätigt: Speichern funktioniert, Bearbeiten funktioniert, Persistenz funktioniert, Export enthält die neu erweiterten Löschkonzept-Felder.
- Technischer Hinweis: Der lokale Dev-Server startet ohne `JWT_SECRET` nicht; für den lokalen Test wurde nur temporär eine lokale Umgebungsvariable gesetzt.
- Arbeitsregel bestätigt: zukünftige Pushes für PrivaShield standardmäßig immer nach `master`.
- Druckexport-Problem mit zu langen Zeilen behoben: Für alle Exporttabellen wurde der Umbruch so angepasst, dass Inhalte im Druck nicht mehr rechts abgeschnitten werden.
- Bugfix-Release durchgeführt: Version auf `1.1.1` erhöht, nach `master` gepusht und GitHub-Branch `1.1.1` angelegt.
- Funktionstest für den Druckexport-Fix erfolgreich durchgeführt, inklusive Test mit langen Löschkonzept-Feldern und erfolgreicher Exportprüfung.
- Neue Arbeitsregel festgehalten: Nach jedem Push und jeder Versionsänderung den aktuellen App-Status aktiv im Memory dokumentieren.
- Druckdarstellung weiter verbessert: breite Exportmodule werden nun im Druck als lesbare Feld/Wert-Blöcke pro Datensatz dargestellt, statt als eine einzige breite Tabellenzeile.
- Kompakte Module wie Aufgaben bleiben tabellarisch, breite fachliche Module werden in die neue Blockdarstellung überführt.
- Bugfix-Release `1.1.2` durchgeführt und nach `master` gepusht; zusätzlicher GitHub-Branch `1.1.2` angelegt.
- Funktionstest für die neue Blockdarstellung erfolgreich durchgeführt.
# 2026-04-20

- PrivaShield weitergeführt und als nächsten sinnvollen Schritt den fachlichen Abschluss rund um Löschkonzept + Export priorisiert.
- Löschkonzept-Codepfad geprüft: Formular, Schema, API, Storage und Export-Kontext sind technisch verdrahtet; kein offensichtlicher harter Save-Blocker im Code gefunden.
- Export für `loeschkonzept` erweitert, sowohl in `client/src/export-page.tsx` als auch in `client/print.html`.
- Druck-/Exportfelder für Löschkonzept erweitert um gesetzliche Frist/Referenz, Aufbewahrungsfrist, Löschereignis, Rechtsgrundlage, Löschverantwortlicher, Systeme/Speicherorte, Kontrolle, Nachweis und Status.
- Löschkonzept-Summary im Druckexport verbessert, u. a. für Fristbezug, Löschereignis, Nachweis und kritische LK5-Fälle.
- Build (`npm run build`) und Typecheck (`npm run check`) erfolgreich durchgelaufen.
- Version von `1.0.0` auf `1.1.0` erhöht.
- Commits erstellt:
  - `2d0105a` `feat: erweitere loeschkonzept-export`
  - `1052acc` `chore: bump version to 1.1.0`
- Änderungen erfolgreich nach `master` gepusht.
- Zusätzlich einen GitHub-Branch `1.1.0` erstellt.
- Funktionstest erfolgreich gegen den laufenden lokalen Server ausgeführt: Login, Erstellen, Bearbeiten, Persistenzprüfung, Export-Kontext-Prüfung und Löschen eines Löschkonzept-Testeintrags.
- Dabei bestätigt: Speichern funktioniert, Bearbeiten funktioniert, Persistenz funktioniert, Export enthält die neu erweiterten Löschkonzept-Felder.
- Technischer Hinweis: Der lokale Dev-Server startet ohne `JWT_SECRET` nicht; für den lokalen Test wurde nur temporär eine lokale Umgebungsvariable gesetzt.
- Arbeitsregel bestätigt: zukünftige Pushes für PrivaShield standardmäßig immer nach `master`.
- Druckproblem im Export bearbeitet: zuerst globalen Wortumbruch für Tabellen eingeführt, danach wegen schlechter Lesbarkeit auf eine mehrzeilige Blockdarstellung für breite Exportmodule umgestellt.
- Breite Exportmodule wie Löschkonzept werden im Druck jetzt als 2-Spalten-Record-Block (Feldname | Wert) statt als überbreite Einzelzeile gerendert; kompakte Module bleiben Tabellen.
- Für den Drucklayout-Fix wurden nacheinander Bugfix-Versionen erhöht und nach `master` gepusht:
  - `1.1.1` mit Commit `a7ec979` `fix: verbessere druckumbruch im export`
  - `1.1.2` mit Commit `d4841f6` `fix: verbessere export-darstellung im druck`
- Zu beiden Ständen wurden GitHub-Branches `1.1.1` und `1.1.2` angelegt.
- Daniel hat als feste Arbeitsregel vorgegeben: nach jedem Push und nach jeder Versionsänderung den aktuellen App-Status im Memory dokumentieren.
- Zuletzt festgestellt: Versionsanzeige in der UI war inkonsistent zum aktuellen Stand; Screenshot zeigte noch 1.1.0, während im Code und Build 1.1.2 geprüft wurde. Wahrscheinliche Ursache: alte laufende Instanz, alter Build oder Browser-Cache. Ein zusätzlicher hart codierter Versionswert in der Systemseite wurde korrigiert, sodass Footer, Plattform-Info und `package.json` auf 1.1.2 konsistent sind.
- Aktueller App-Status am Ende der Sitzung: Repository-Stand ist auf `master` aktuell, Versionsstand im Code ist `1.1.2`, Druckexport für breite Module ist auf lesbare Blockdarstellung umgestellt, und es lagen zuletzt keine weiteren uncommitteten Projektänderungen im PrivaShield-Repo vor.
- Backup-Scheduler als integrierte App-Funktion weiter ausgebaut: serverseitige interne Aktivierung beim Serverstart ergänzt, Statusfelder für letzten Lauf/Erfolg/Fehler und Scheduler-Aktivität eingeführt, Config-API erweitert und Backup-UI um Statusanzeige ergänzt.
- Echten Funktionstest für den Backup-Bereich erfolgreich durchgeführt: Login, Config lesen, automatische Backups aktivieren, Config speichern, manuellen Backup-Lauf auslösen, Statusfelder validieren und erzeugte Backup-Dateien prüfen.
- Feature-Release durchgeführt: Version auf `1.2.0` erhöht, Commit `ec5e7e9` (`feat: integriere backup-scheduler in die app`) erstellt, nach `master` gepusht und Branch `1.2.0` auf GitHub angelegt.
- Backup-Statusbereich anschließend i18n-sauber überführt: neue Scheduler-/Status-/Hinweis-Texte in DE und EN ergänzt und harte Strings im Backup-UI ersetzt.
- Sprachkonsistenz der neuen Backup-Keys erfolgreich geprüft.
- Bugfix-Release durchgeführt: Version auf `1.2.1` erhöht, Commit `674d60d` (`fix: internationalisiere backup-statusbereich`) erstellt, nach `master` gepusht und Branch `1.2.1` auf GitHub angelegt.
- Anschließend restliche Backup-Übersetzungen vervollständigt: Rotationshinweis, Unterzeile zu erkannten Sicherungen, Slot-Bezeichnungen und Status-Badges im Backup-Bereich i18n-sauber gemacht.
- Bugfix-Release durchgeführt: Version auf `1.2.2` erhöht, Commit `3e6fd0d` (`fix: vervollstaendige backup-uebersetzungen`) erstellt, nach `master` gepusht und Branch `1.2.2` auf GitHub angelegt.
- Nach realem Scheduler-Fehlerbild die Betriebsanforderung geschärft: automatische verschlüsselte Backups benötigen zwingend `PRIVASHIELD_BACKUP_PASSWORD` in der Laufzeitumgebung.
- Relevante UI-Texte im Backup-Bereich präzisiert und verbleibende Fehlermeldungen/Platzhalter i18n-sauber gemacht.
- README, API-Doku und Unraid-Doku um klare Hinweise für automatische verschlüsselte Scheduler-Backups ergänzt.
- Bugfix-/Doku-Release durchgeführt: Version auf `1.2.3` erhöht, Commit `d774609` (`docs: klaere backup-passwort fuer scheduler`) erstellt, nach `master` gepusht und Branch `1.2.3` auf GitHub angelegt.
- Zusätzlich sichtbare Warnbox im Backup-Bereich ergänzt, wenn Verschlüsselung und automatische Backups aktiv sind und das Scheduler-Laufzeitkennwort wahrscheinlich fehlt.
- Bugfix-Release durchgeführt: Version auf `1.2.4` erhöht, Commit `d5f4c1b` (`fix: ergaenze backup-warnung fuer scheduler-passwort`) erstellt, nach `master` gepusht und Branch `1.2.4` auf GitHub angelegt.
- Backup-Wiederherstellung funktional erweitert: Restore aus vorhandener Backup-Liste plus Upload externer `.bak`- und `.enc`-Dateien, inklusive Passwortabfrage für verschlüsselte Backups.
- Feature-Release durchgeführt: Version auf `1.2.5` erhöht, Commit `ecab9ce` (`feat: ergaenze backup-restore per upload`) erstellt, nach `master` gepusht und Branch `1.2.5` auf GitHub angelegt.
- Warnlogik für fehlendes Scheduler-Laufzeitkennwort korrigiert: Hinweis erscheint jetzt nur noch bei tatsächlich fehlendem `PRIVASHIELD_BACKUP_PASSWORD` und nicht mehr wegen historischer Fehlermeldungen.
- Bugfix-Release durchgeführt: Version auf `1.2.6` erhöht, Commit `b6354f8` (`fix: zeige backup-warnung nur bei fehlendem runtime-passwort`) erstellt, nach `master` gepusht und Branch `1.2.6` auf GitHub angelegt.
- Backup-Konfiguration weiter bereinigt: lokales Backup-Kennwort-Feld entfernt, zugehörige API-/Schema-/Serverlogik gestrichen, damit die Konfiguration nur noch die tatsächlich relevanten Backup-Parameter enthält.
- Bugfix-Release durchgeführt: Version auf `1.2.7` erhöht, Commit `2c3ea71` (`fix: entferne backup-kennwort aus konfiguration`) erstellt, nach `master` gepusht und Branch `1.2.7` auf GitHub angelegt.

- 2026-04-20: Backup-i18n im Backup-Statusbereich bereinigt, Resttexte in Backup-Konfiguration und Existing-Backups ebenfalls internationalisiert; Version 1.2.1 (`674d60d`, `fix: internationalisiere backup-statusbereich`) und 1.2.2 (`3e6fd0d`, `fix: vervollstaendige backup-uebersetzungen`) nach `master` gepusht, Release-Branches `1.2.1` und `1.2.2` angelegt.
- 2026-04-20: Laufzeitfehler für automatische verschlüsselte Backups analysiert, Ursache ist fehlende Umgebungsvariable `PRIVASHIELD_BACKUP_PASSWORD` beim Scheduler. Entscheidung: Option 1 umsetzen und Dokumentation/Texte darauf ausrichten.
- 2026-04-20: Relevante UI-/Dokutexte für Scheduler-Backups präzisiert, README/API-/Unraid-Doku ergänzt, dass automatische verschlüsselte Backups zwingend `PRIVASHIELD_BACKUP_PASSWORD` benötigen; Version 1.2.3 (`d774609`, `docs: klaere backup-passwort fuer scheduler`) nach `master` gepusht, Release-Branch `1.2.3` angelegt.
