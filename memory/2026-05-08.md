- PrivaShield Release-Schritt auf Version 1.10.1 abgeschlossen.
- Inhaltlich wurde die Cookie-/Bearer-Auth um CSRF-Schutz erweitert: Backend erzeugt und prüft jetzt ein separates `privashield_csrf`-Token für schreibende Requests, `logout` ist CSRF-geschützt, und `login` sowie `/api/auth/me` liefern frische CSRF-Tokens aus.
- Frontend ergänzt: `client/src/lib/queryClient.ts` sendet bei zustandsändernden Requests automatisch `X-CSRF-Token`; `AuthProvider` übernimmt und aktualisiert das CSRF-Token bei Login, Session-Restore und Logout.
- Zusätzlich wurde der bestehende Backup/Restore-Integrationstest stabilisiert und in `server/db.ts` ein Drift zwischen `shared/schema.ts` und SQLite-Bootstrap für die VVT-Risikofelder (`risikostufe`, `risikobegruendung`, `risiko_triggers`, `risikopruefung_am`) behoben; defensive `ALTER TABLE`-Nachzüge für Bestandsdatenbanken ergänzt.
- Verifikation für 1.10.1 erfolgreich: `npm test`, `npm run check` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün. Teststand danach: 12 Dateien / 45 Tests erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `a360572` (`fix: add csrf protection to cookie auth flow`). Zusätzlicher Branch `1.10.1` erstellt und gepusht.
- Folge-Härtung als Version 1.10.2 veröffentlicht.
- Ergänzend zum CSRF-Token prüft der Server bei zustandsändernden Requests jetzt auch `Origin` bzw. `Referer` gegen die erwartete Ziel-Origin; dabei werden Proxy-Header (`x-forwarded-host`, `x-forwarded-proto`) berücksichtigt, damit reale Reverse-Proxy-/Unraid-Deployments korrekt funktionieren.
- Zusätzliche Security-Tests decken jetzt sowohl blockierte Requests bei fremder Origin als auch erlaubte Requests bei passender Origin plus gültigem CSRF-Token ab.
- Verifikation für 1.10.2 erfolgreich: `npm test`, `npm run check` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün. Teststand danach: 12 Dateien / 47 Tests erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `cee1892` (`fix: validate origin on csrf-protected requests`). Zusätzlicher Branch `1.10.2` erstellt und gepusht.
- Doku- und Unraid-Update als Version 1.10.3 veröffentlicht.
- README, DOCKER-README und Unraid-Anleitung wurden um produktionsrelevante Hinweise zu Cookie-Auth, Bearer-Fallback, CSRF-Token, Origin-/Referer-Prüfung, Reverse-Proxy-Headern sowie Troubleshooting für `401`/`403` ergänzt.
- Zusätzlich wurde das Unraid-Icon auf Basis des PrivaShield-Favicon-Stils neu gestaltet; `assets/unraid-icon.png` ist jetzt ein echtes 512×512-PNG und `assets/unraid-icon.svg` wurde entsprechend modernisiert.
- Verifikation für 1.10.3 erfolgreich: `npm test`, `npm run check` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün. Teststand blieb bei 12 Dateien / 47 Tests erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `a1270e3` (`docs: refresh unraid deployment guidance and icon`). Zusätzlicher Branch `1.10.3` erstellt und gepusht.
- Kleiner Compose-/Unraid-Fix als Version 1.10.4 veröffentlicht.
- `docker-compose.yml` und `docker-compose.unraid.yml` veröffentlichen jetzt explizit sowohl `com.unraid.icon` als auch `net.unraid.docker.icon` mit Verweis auf das echte PrivaShield-Unraid-Icon, damit die Symbolanzeige in Unraid konsistenter erzwungen wird. Gleichzeitig wurde das vorherige generische Node-Logo im regulären Compose ersetzt.
- Verifikation für 1.10.4 erfolgreich: `npm test`, `npm run check` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün. Teststand blieb bei 12 Dateien / 47 Tests erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `f33c14f` (`fix: publish unraid icon labels in compose files`). Zusätzlicher Branch `1.10.4` erstellt und gepusht.
- Datenpannen-Workflow-Upgrade als Version 1.10.5 veröffentlicht.
- Die Datenpannen-Seite wurde operativ deutlich ausgebaut: neue Fokusliste für fristkritische, meldepflichtige, Art.-34-relevante und sonstige kritische/hohe Vorfälle; zusätzliche URL-stabile Filter (`critical`, `reportable-open`, `deadline-72h`, `deadline-missed`, `notify-art34`, `open`) und Sortierungen für Frist, Schwere, Neueste und Titel.
- Neu ist außerdem eine echte 72h-/Meldepflicht-Warnlogik direkt in der Oberfläche: Aus `frist72h` bzw. Entdeckungszeitpunkt werden Friststatus, offene Behördenmeldung, Fristüberschreitung und möglicher Art.-34-Benachrichtigungsbedarf abgeleitet und als Arbeitszustand visualisiert.
- Für priorisierte Datenpannen lassen sich jetzt direkt Folgeaufgaben und PDCA-Zyklen erzeugen; inklusive Duplikat-Schutz über `vorlagenBezug` bzw. PDCA-Source, automatischer Priorisierung und vorbelegten Beschreibungen je Eskalationsart.
- Verifikation für 1.10.5 erfolgreich: `npm run check`, `npm test` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün. Ein zwischenzeitlicher einmaliger Timeout in `server/backup.restore.test.ts` erwies sich nach Einzeltest und erneutem Gesamtlauf als flakey, nicht als reproduzierbare Regression. Teststand am Ende: 12 Dateien / 47 Tests erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `e4c87ee` (`feat: add breach focus workflows`). Zusätzlicher Branch `1.10.5` erstellt und gepusht.
- Dashboard-Eskalation für Datenpannen als Version 1.10.6 veröffentlicht.
- Das zentrale Dashboard berücksichtigt jetzt auch Datenpannen direkt in den priorisierten Governance-Hinweisen und Warnungen: überschrittene 72h-Fristen, aktive 72h-Fristen, offene meldepflichtige Vorfälle, Art.-34-Prüfbedarf sowie kritische/hohe Datenpannen werden mandantenbezogen eingeblendet und direkt ins gefilterte Datenpannen-Modul verlinkt.
- Dafür lädt das Dashboard nun zusätzlich die Datenpannen des aktiven Mandanten und bewertet sie mit derselben 72h-/Meldepflicht-Logik wie im Modul selbst, sodass Frist- und Meldehinweise konsistent an beiden Stellen erscheinen.
- Verifikation für 1.10.6 erfolgreich: `npm run check`, `npm test` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün. Teststand blieb bei 12 Dateien / 47 Tests erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `041ac58` (`feat: elevate breach alerts to dashboard`). Zusätzlicher Branch `1.10.6` erstellt und gepusht.
- AVV-Workflow-Upgrade als Version 1.10.7 veröffentlicht.
- Die AVV-Seite wurde operativ deutlich erweitert: neue Fokusliste für überfällige Reviews, fehlende Prüftermine, SCC-/Drittlandprüfbedarf, Copilot-/Microsoft-AVV ohne Reviewtermin und nicht aktive Vertragsstände; zusätzliche URL-stabile Filter (`review-overdue`, `review-missing`, `scc-missing`, `inactive`, `copilot`, `copilot-missing-review`) sowie Sortierungen für Review, Name und Status.
- Neu sind außerdem direkte Aufgaben- und PDCA-Folgeaktionen für priorisierte AVV-Fälle inklusive Duplikat-Schutz über `vorlagenBezug` bzw. PDCA-Source sowie vorbelegte fachliche Beschreibungen je Review-/Transfer-/Statusfall.
- Die Warnlogik bewertet jetzt AVV-spezifisch Prüffälligkeit, fehlende Reviewtermine, mögliche SCC-/Drittlandkontexte und Copilot-/Microsoft-Sonderfälle direkt in der Oberfläche.
- Verifikation für 1.10.7 erfolgreich: `npm run check`, `npm test` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün. Teststand blieb bei 12 Dateien / 47 Tests erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `da577cf` (`feat: add avv focus workflows`). Zusätzlicher Branch `1.10.7` erstellt und gepusht.
- Dashboard-Eskalation für AVV als Version 1.10.8 veröffentlicht.
- Das zentrale Dashboard berücksichtigt jetzt auch AVV-relevante Governance-Risiken direkt in den priorisierten Hinweisen und Warnungen: überfällige AVV-Reviews, offener SCC-/Transferprüfbedarf, Copilot-/Microsoft-AVV ohne Prüffälligkeit, fehlende Reviewtermine sowie AVV mit nicht aktivem Status werden direkt angezeigt und in die passenden gefilterten AVV-Ansichten verlinkt.
- Damit ist die zentrale Governance-Sicht jetzt über VVT, DSFA, Datenpannen und AVV konsistenter; AVV-spezifische Review-/Transfer-Themen erscheinen nicht mehr nur im Modul, sondern auch in der übergreifenden Prioritätenansicht.
- Verifikation für 1.10.8 erfolgreich: `npm run check`, `npm test` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün. Teststand blieb bei 12 Dateien / 47 Tests erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `8c7136c` (`feat: elevate avv alerts to dashboard`). Zusätzlicher Branch `1.10.8` erstellt und gepusht.
- Security-/Docker-Härtung als Version 1.10.9 veröffentlicht.
- Abhängigkeiten gezielt gegen die gemeldeten Vulnerabilities bereinigt: `drizzle-orm` auf `^0.45.2` aktualisiert, `drizzle-kit` auf `^0.31.10` nachgezogen und der Lockfile-Bestand bereinigt; im Audit-Check ergab sich danach für den relevanten Satz kein verbleibender High/Critical-Befund mehr.
- Docker-Härtung nachgezogen: Baseimage in Build- und Runtime-Stage explizit auf `node:20-alpine3.22` gesetzt, zusätzlich `apk update && apk upgrade --no-cache` integriert, Compose-/Unraid-Container mit `build.pull: true`, `read_only: true` und zusätzlichen `tmpfs`-Mounts für `/tmp` und `/run` verschärft.
- Besonderheit bei der Verifikation: Wegen global gesetztem `NODE_ENV=production` wurden Dev-Dependencies zwischenzeitlich lokal nicht installiert; für die saubere Prüfung wurde deshalb bewusst mit Development-Installationskontext verifiziert. Endergebnis blieb grün.
- Verifikation für 1.10.9 erfolgreich: `npm run check`, `npm test` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün. Teststand blieb bei 12 Dateien / 47 Tests erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `c935415` (`fix: harden docker image and dependency stack`). Zusätzlicher Branch `1.10.9` erstellt und gepusht.
- TOM-Fokuslogik und Aufgaben-/PDCA-Dashboard-Veredelung als Feature-Release `1.11.0` veröffentlicht.
- TOM-Modul in `client/src/App.tsx` fachlich deutlich aufgewertet: neue Fokusliste mit priorisierten Problemfällen, Filter für `review-overdue`, `review-missing`, `planned`, `weak-effectiveness` und `missing-owner`, Status-/Review-Sortierung sowie sichtbare Warn-Badges direkt in der Maßnahmenliste.
- Für priorisierte TOM-Fälle wurden direkte Folgeaktionen integriert: automatische Aufgabenerzeugung mit Duplikat-Schutz über `vorlagenBezug`, vorbereitete Aufgaben-Drafts per Deep-Link sowie automatische PDCA-Zyklen mit Folgeaufgabe für Review-, Umsetzungs-, Wirksamkeits- und Verantwortungsdefizite.
- Das zentrale Dashboard wurde operativ geschärft: zusätzliche Governance-Hinweise für TOM-Review-Rückstände, geplante TOM ohne Abschluss, fehlende Prüftermine, schwache/offene Wirksamkeit und fehlende Verantwortlichkeit; außerdem stärkere Aufgaben-/PDCA-Steuerung mit Hinweisen auf überfällige PDCA-Folgeaufgaben, offene Hochprioritäts-PDCA, offene Zyklen ohne Prüftermin, laufende Zyklen ohne Folgeaufgabe sowie kritische Aufgaben ohne Fälligkeit.
- Damit ist die Governance-Logik jetzt noch konsistenter über Aufgaben, PDCA, TOM, AVV, Datenpannen, DSFA und VVT verzahnt; insbesondere lassen sich operative Folgezyklen im Dashboard besser priorisieren und in die fachlich passenden Module abspringen.
- Verifikation für `1.11.0` erfolgreich: `npm run check`, `npm test` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün. Teststand blieb bei 12 Dateien / 47 Tests erfolgreich; `server/backup.restore.test.ts` lief diesmal im Gesamtlauf spürbar langsamer, aber weiterhin erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `e4dd728` (`feat: add tom focus workflows and dashboard escalation`). Zusätzlicher Branch `1.11.0` erstellt und gepusht.
- PDCA-Filterlogik nachgezogen und als Patch-Release `1.11.1` veröffentlicht.
- Hintergrund: Das Dashboard verlinkte bereits auf neue PDCA-Fokusrouten (`priority-high`, `review-missing`, `in-progress-no-task`), die PDCA-Seite selbst filterte aber bislang nur `review` und `audit-follow-up-ohne-audit` explizit. Dadurch waren neue Governance-Links semantisch richtig, aber noch nicht vollständig zielgenau.
- In `client/src/App.tsx` wurde die PDCA-Seite deshalb fachlich und technisch nachgeschärft: neue Filterauswertung für `priority-high`, `review-missing` und `in-progress-no-task`, zentrale `getPdcaMeta(...)`-Ableitung für Prüftermin fehlt/fällig, Audit-Follow-up ohne Audit, hohe Priorität sowie laufende Zyklen ohne offene Folgeaufgabe.
- Zusätzlich wurden Hinweistext, KPI-Kacheln und Warnboxen der PDCA-Seite erweitert, damit gefilterte Ansichten den jeweiligen Governance-Zustand nun auch sichtbar erklären und nicht nur still filtern.
- Die Arbeitszustandsanzeige einzelner PDCA-Karten unterscheidet jetzt gezielter zwischen `priorisiert`, `blockiert`, `Termin festlegen`, `in Bearbeitung`, `neu` und `beobachten`.
- Verifikation für `1.11.1` erfolgreich: `npm run check`, `npm test` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün. Teststand blieb bei 12 Dateien / 47 Tests erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `a76f555` (`feat: align pdca filters with dashboard routes`). Zusätzlicher Branch `1.11.1` erstellt und gepusht.
- Aufgaben-Fokusfilter für den nächsten Governance-Block als Patch-Release `1.11.2` veröffentlicht.
- Hintergrund: Nach der Nachschärfung der PDCA-Filter blieben auf der Aufgaben-Seite noch zwei operative Fokusfälle nur indirekt sichtbar: `kritische Aufgaben ohne Fälligkeit` und `überfällige PDCA-Folgeaufgaben`. Das Dashboard erkannte diese Zustände bereits, sprang aber noch nicht in eigenständige hart gefilterte Aufgabenansichten.
- In `client/src/App.tsx` wurde die Aufgaben-Seite deshalb erweitert um zwei explizite URL-Filterrouten: `kritisch-ohne-faelligkeit` und `pdca-follow-up-ueberfaellig`.
- Zusätzlich wurden die Filterhinweise, KPI-Kacheln und Arbeitszustände in der Aufgabenansicht erweitert, sodass die Governance-Fokusfälle nicht nur technisch gefiltert, sondern für den Nutzer auch operativ klar erklärt werden.
- Dashboard-Links für `kritische Aufgaben ohne Fälligkeit` und `überfällige PDCA-Folgeaufgaben` zeigen jetzt direkt auf diese präzisen Aufgabenfilter statt nur auf allgemeinere Sammelansichten.
- Verifikation für `1.11.2` erfolgreich: `npm run check`, `npm test` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün. Teststand blieb bei 12 Dateien / 47 Tests erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `91c6538` (`feat: add task focus filters for governance follow-up`). Zusätzlicher Branch `1.11.2` erstellt und gepusht.
- Regression-Absicherung für Dashboard-Filterrouten als Stabilitäts-Patch `1.11.3` veröffentlicht.
- Nach systematischer Prüfung der aktuellen Dashboard-Deep-Links zeigte sich kein weiterer akuter Routing-Bruch in den Fachmodulen; statt künstlich neue Filter zu bauen, wurde deshalb ein gezielter Regressionstest ergänzt, damit die bereits nachgezogene Governance-Filterlogik nicht bei späteren Umbauten still wieder auseinanderläuft.
- Neue Testdatei `server/dashboard-filter-regression.test.ts` prüft statisch gegen `client/src/App.tsx`, dass zentrale Dashboard-Links weiterhin auf unterstützte Modulrouten zeigen und die zuletzt eingeführten harten Governance-Filter für Aufgaben (`kritisch-ohne-faelligkeit`, `pdca-follow-up-ueberfaellig`) sowie PDCA (`priority-high`, `review-missing`, `in-progress-no-task`) explizit sowohl als Dashboard-Link als auch als Seitenfilter im Code vorhanden bleiben.
- Verifikation für `1.11.3` erfolgreich: Gesamtlauf `npm test`, `npm run check` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün. Teststand stieg durch den neuen Guard auf 13 Dateien / 50 Tests erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `0ce3d2d` (`test: guard dashboard filter routes`). Zusätzlicher Branch `1.11.3` erstellt und gepusht.
- DSR-/Betroffenenrechte-Modul fachlich ausgebaut und als Feature-Release `1.12.0` veröffentlicht.
- Das bislang eher einfache DSR-Modul wurde in `client/src/App.tsx` operativ deutlich erweitert: neue Fokusliste für `deadline-missed`, `deadline-soon`, `missing-deadline`, `verification` und `open`, URL-stabile Filter, Sortierungen nach Frist/Neueste/Art sowie deutlichere Warn-Badges direkt in der Anfragenliste.
- Für priorisierte DSR-Fälle wurden direkte Folgeaktionen ergänzt: automatische Aufgabenerzeugung mit Duplikat-Schutz über `vorlagenBezug`, vorbereitete Aufgaben-Drafts per Deep-Link sowie automatische PDCA-Zyklen mit Folgeaufgabe für Fristverletzungen, bald auslaufende Fristen, fehlende Fristdokumentation und Identifikations-/Prüfstatus.
- Zusätzlich wurde das zentrale Dashboard um DSR-Governance-Hinweise ergänzt: überschrittene DSR-Fristen, kurzfristig auslaufende Fristen, offene DSR ohne dokumentierte Frist und DSR im Prüf-/Identifikationsstatus werden jetzt direkt in den priorisierten Hinweisen und Warnungen angezeigt und in die passenden DSR-Filteransichten verlinkt.
- Der zuvor eingeführte Dashboard-Filter-Regressionstest wurde entsprechend erweitert, damit jetzt auch DSR-Dashboard-Routen als unterstützte Zielmodule abgesichert bleiben.
- Verifikation für `1.12.0` erfolgreich: `npm run check`, `npm test` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün. Teststand blieb bei 13 Dateien / 50 Tests erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `d2776b9` (`feat: add dsr focus workflows and dashboard alerts`). Zusätzlicher Branch `1.12.0` erstellt und gepusht.
- Löschkonzept-Modul fachlich ausgebaut und als Feature-Release `1.13.0` veröffentlicht.
- Das Löschkonzept wurde in `client/src/App.tsx` operativ deutlich erweitert: neue Fokuslogik für `missing-trigger`, `missing-owner`, `missing-proof`, `lk5-control` und `free-period`, URL-stabile Quick-Filter sowie sichtbare Warn-Badges direkt in der Eintragsliste.
- Für priorisierte Löschkonzept-Fälle wurden direkte Folgeaktionen ergänzt: automatische Aufgabenerzeugung mit Duplikat-Schutz über `vorlagenBezug`, vorbereitete Aufgaben-Drafts per Deep-Link sowie automatische PDCA-Zyklen mit Folgeaufgabe, insbesondere für fehlende Löschereignisse, offene Verantwortlichkeiten, fehlende Nachweise/Kontrollen, freie Fristkategorien und LK5-Hochrisikoeinträge.
- Zusätzlich wurde das zentrale Dashboard um Löschkonzept-Governance-Hinweise ergänzt: LK5-Kontrollbedarf, fehlende Löschereignisse, fehlende Löschverantwortung, fehlende Nachweis-/Kontrolllogik und freie Fristkategorien werden jetzt direkt in den priorisierten Hinweisen angezeigt und in die passenden Löschkonzept-Filteransichten verlinkt.
- Der Dashboard-Filter-Regressionstest wurde entsprechend erweitert, damit nun auch Löschkonzept-Dashboard-Routen als unterstützte Zielmodule abgesichert bleiben.
- Verifikation für `1.13.0` erfolgreich: `npm run check`, `npm test` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün. Teststand blieb bei 13 Dateien / 50 Tests erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `e5a50a9` (`feat: add retention governance workflows`). Zusätzlicher Branch `1.13.0` erstellt und gepusht.
- Daniel hat direkt im Anschluss beauftragt, die nächsten zwei schwächeren Fachblöcke ohne Pause gemeinsam fertigzustellen; umgesetzt wurden daraufhin KI-Compliance und Audit als gemeinsamer Feature-Block.
- KI-Compliance wurde in `client/src/App.tsx` operativ deutlich ausgebaut: Die Seite enthält jetzt priorisierte Fokuslogik für `missing-policy`, `missing-ai-act`, `incomplete-checklist`, `dsfa-gap` und `tool-inventory-gap`, sichtbare Governance-Hinweise, vorbereitete Aufgaben-Deep-Links sowie direkte Aufgaben- und PDCA-Folgeaktionen mit Duplikat-Schutz über `vorlagenBezug` bzw. PDCA-Source.
- Audit wurde parallel fachlich nachgeschärft: neue URL-stabile Fokusfilter `missing-follow-up`, `overdue-next-audit`, `critical-open`, `missing-owner` und `in-progress-no-task`, dazu KPI-/Hinweislogik, Arbeitszustände auf Karten sowie eine Audit-Fokusliste für Abweichungen ohne PDCA-Follow-up, überfällige Auditzyklen, offene kritische Ergebnisse, fehlende Verantwortlichkeiten und laufende Follow-ups ohne Folgeaufgabe.
- Das zentrale Dashboard wurde zusätzlich um KI- und Audit-Governance erweitert. Neu priorisiert/verlinkt werden dort jetzt insbesondere KI-Einsatz ohne KI-Richtlinie, fehlende KI-VO-Prüfung, offene DSFA-Lücke, unvollständige DSGVO-Checkliste, fehlendes KI-Toolinventar sowie Audit-Fälle ohne PDCA-Follow-up, offene hohe/kritische Audits, überfällige nächste Audit-Termine, fehlende Verantwortlichkeiten und Audit-Follow-ups ohne offene Folgeaufgabe.
- Der Regressionstest `server/dashboard-filter-regression.test.ts` wurde erweitert, damit Dashboard-Zielrouten nun zusätzlich auch `/ki-compliance?...` und `/audits?...` als zulässige Governance-Ziele absichern.
- Während der Umsetzung gab es einen kurzen Scope-Fehler im Dashboard (`Cannot find name 'audits'` in `client/src/App.tsx`); Ursache war eine fehlende `useModuleData("audits")`-Query im Dashboard-Kontext. Nach Ergänzung lief der gesamte Prüfpfad wieder grün.
- Verifikation für `1.14.0` erfolgreich: `npm run check`, `npm test` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen vollständig grün. Teststand blieb bei 13 Dateien / 50 Tests erfolgreich.
- Feature-Release `1.14.0` veröffentlicht. Änderungen erfolgreich nach `master` gepusht. Commit: `71dc738` (`feat: expand ai and audit governance workflows`). Zusätzlicher Branch `1.14.0` erstellt und gepusht.
- Nächster Fachblock direkt anschließend: Beschäftigtendatenschutz als weiterer bislang eher formularlastiger Bereich operativ ausgebaut und als `1.15.0` veröffentlicht.
- Beschäftigtendatenschutz wurde in `client/src/App.tsx` fachlich nach dem etablierten Governance-Muster erweitert: Fokuslogik für `training-overdue`, `missing-privacy-notice`, `missing-confidentiality`, `missing-telecom`, `missing-training`, `missing-evidence` und `missing-target-groups` ergänzt.
- Zusätzlich wurden direkte Folgeaktionen eingebaut: vorbereitete Aufgaben-Deep-Links, automatische Aufgabenerzeugung mit Duplikat-Schutz über `vorlagenBezug` sowie automatische PDCA-Zyklen mit Folgeaufgabe für priorisierte Lücken im Beschäftigtendatenschutz.
- Das Dashboard berücksichtigt jetzt auch Beschäftigtendatenschutz-Governance mit priorisierten Hinweisen und Deep-Links für überfällige Schulungen, fehlende Datenschutzerklärung, fehlende Vertraulichkeitsverpflichtung, fehlende Telekommunikationsverpflichtung, fehlende Schulungsdokumentation, fehlende Nachweise und nicht definierte Zielgruppen.
- Der Regressionstest `server/dashboard-filter-regression.test.ts` wurde erneut erweitert, damit zusätzlich auch `/beschaeftigtendatenschutz?...` als zulässiges Dashboard-Ziel abgesichert bleibt.
- Verifikation für `1.15.0` erfolgreich: `npm run check`, `npm test` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen vollständig grün. Teststand blieb bei 13 Dateien / 50 Tests erfolgreich.
- Feature-Release `1.15.0` veröffentlicht. Änderungen erfolgreich nach `master` gepusht. Commit: `3a60874` (`feat: add employee privacy governance workflows`). Zusätzlicher Branch `1.15.0` erstellt und gepusht.
- Direkt anschließend wurde der Web-Datenschutz-/Datenschutzhinweis-Block als nächster schwächerer Fachbereich operativ ausgebaut und als `1.16.0` veröffentlicht.
- In `client/src/App.tsx` wurden für Web-Datenschutz und Datenschutzhinweise Fokuslogiken ergänzt für `consent-missing`, `consent-reason-missing`, `privacy-notice-missing`, `impressum-missing`, `groups-missing`, `distribution-missing` und `notes-missing`.
- Der Web-Block erhielt zusätzlich vorbereitete Aufgaben-Deep-Links, direkte Aufgabenanlage mit Duplikat-Schutz über `vorlagenBezug` sowie automatische PDCA-Zyklen mit Folgeaufgabe für priorisierte Lücken rund um Consent-Management, Datenschutzerklärung, Impressum und Bereitstellungslogik der Datenschutzhinweise.
- Das zentrale Dashboard berücksichtigt jetzt auch Web-Datenschutz-Governance: fehlendes erforderliches Consent-Tool, fehlende Consent-Begründung, ungeprüfte Datenschutzerklärung, ungeprüftes Impressum, fehlende Personengruppen, fehlender Bereitstellungsweg und fehlende Dokumentationsnotizen werden priorisiert angezeigt und verlinkt.
- Der Regressionstest `server/dashboard-filter-regression.test.ts` wurde erneut erweitert, damit zusätzlich auch `/web-datenschutz?...` als zulässiges Dashboard-Ziel abgesichert bleibt.
- Verifikation für `1.16.0` erfolgreich: `npm run check`, `npm test` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen vollständig grün. Teststand blieb bei 13 Dateien / 50 Tests erfolgreich.
- Feature-Release `1.16.0` veröffentlicht. Änderungen erfolgreich nach `master` gepusht. Commit: `aa50500` (`feat: expand web privacy governance workflows`). Zusätzlicher Branch `1.16.0` erstellt und gepusht.
- Direkt danach meldete Daniel einen akuten Fehler im Live-Betrieb: neue Einträge konnten modulübergreifend nicht mehr gespeichert werden; es erschien nur noch generisch „Fehler“.
- Analyseentscheidung: Da der Fehler „nirgendwo speichern“ betraf und Backend-/Route-Tests grün blieben, wurde die gemeinsame Frontend-Mutationslogik `useModuleData(...)` als zentraler Verdächtiger untersucht und gehärtet statt einzelne Formulare isoliert zu patchen.
- Hotfix `1.16.1` umgesetzt: `useModuleData(...)` in `client/src/App.tsx` zentral robuster gemacht mit gemeinsamer `moduleBase`-Route, Guards für fehlenden aktiven Mandanten und fehlende IDs bei Update/Delete sowie sauberer Behandlung von `204 No Content`-Antworten für Create/Update. Ziel war, globale Save-/Mutation-Pfade wieder belastbar zu machen.
- Verifikation für `1.16.1` erfolgreich: `npm run check`, `npm test` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen vollständig grün. Teststand blieb bei 13 Dateien / 50 Tests erfolgreich.
- Hotfix `1.16.1` veröffentlicht. Änderungen erfolgreich nach `master` gepusht. Commit: `a5c9ef3` (`fix: harden module save mutations`). Zusätzlicher Branch `1.16.1` erstellt und gepusht.
- Danach wurde ein weiterer, zuvor noch lokaler Live-Fix separat als Hotfix `1.16.2` veröffentlicht: `client/src/lib/queryClient.ts` liest bei schreibenden Requests das CSRF-Token jetzt zusätzlich live aus dem Browser-Cookie `privashield_csrf` und synchronisiert den Token nach Requests erneut. Hintergrund: reale Browser-Sessions konnten mit einem im State veralteten CSRF-Token weiterarbeiten, obwohl das Cookie bereits erneuert worden war.
- Ziel von `1.16.2`: Schreibende Requests (`POST`/`PUT`/`DELETE`) robuster gegen CSRF-Token-Drift zwischen React-State und Cookie machen, um den globalen Save-Fehler im Live-Betrieb zu beheben.
- Hotfix `1.16.2` erfolgreich nach `master` gepusht. Commit: `1808a68` (`fix: refresh csrf token from cookie on writes`). Zusätzlicher Branch `1.16.2` erstellt und gepusht.
- Danach wurde der weiterhin gemeldete Live-Save-Fehler anhand des AVV-Dialogs weiter eingegrenzt. Technischer Kernfehler: Das AVV-Formular in `client/src/App.tsx` sendete inzwischen zusätzliche Felder (`datenarten`, `betroffenePersonen`, `technischeMassnahmen`, `pruefintervall`, `subauftragnehmerHinweis`), die im gemeinsamen AVV-Schema und im SQLite-Bootstrap nicht vollständig verdrahtet waren. Dadurch konnte der Save-Request serverseitig an der Validierung bzw. Persistenz scheitern.
- Hotfix `1.16.3` umgesetzt: `shared/schema.ts` um die fehlenden AVV-Felder erweitert, `server/db.ts` für Neuaufbau und bestehende SQLite-Instanzen per `safeAlter(...)` nachgezogen und die LowDB-Vorlagen-/Import-Persistenz in `server/storage-lowdb.ts` auf die neuen Felder angepasst.
- Verifikation für `1.16.3` erfolgreich: `npm run check` ✅, `npm test` ✅, `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` ✅.
- Hotfix `1.16.3` erfolgreich nach `master` gepusht. Commit: `488b03a` (`fix: align avv save schema with form fields`). Zusätzlicher Branch `1.16.3` erstellt und gepusht.
- Danach wurde der Fehler appweit neu bewertet: Nicht nur mögliche Einzelfehler in Modulen, sondern auch die bisher zu grobe Fehlerbehandlung im Frontend war hinderlich, weil reale Backend-Validierungs- oder Persistenzfehler überall nur als generisches Toast `Fehler` angezeigt wurden.
- Hotfix `1.16.4` umgesetzt: `client/src/lib/queryClient.ts` wertet Backend-Fehlerantworten jetzt strukturierter aus (`message`, Zod-`issues`) und erzeugt daraus eine konkrete Exception-Nachricht. Zusätzlich wurden die generischen Save-Toast-Pfade in mehreren Kernmodulen (u. a. VVT, AVV, DSFA, Datenpannen, DSR, TOM, Löschkonzept, Dokumente) so angepasst, dass die echte Backend-Fehlermeldung im UI angezeigt wird statt nur `Fehler`.
- Ziel von `1.16.4`: appweiten Save-Fehler nicht mehr blind behandeln, sondern echte Ursachen im Live-Betrieb sofort sichtbar machen und dadurch weitere systematische Persistenz-/Schemafehler schnell identifizieren.
- Verifikation für `1.16.4` erfolgreich: `npm run check` ✅, `npm test` ✅, `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` ✅.
- Hotfix `1.16.4` erfolgreich nach `master` gepusht. Commit: `41c7da3` (`fix: surface backend save errors across modules`). Zusätzlicher Branch `1.16.4` erstellt und gepusht.
- PrivaShield Zwischenstand nach Release `1.13.0`: Daniel hat direkt danach beauftragt, die beiden nächsten schwächeren Fachblöcke ohne Pause nacheinander umzusetzen; als Reihenfolge wurde KI-Compliance vor Audit festgelegt, weil KI-Compliance im UI noch schwächer ausgebaut ist als das Audit-Modul.
- Operative Arbeitsentscheidung für die nächste Sessionphase: gleicher Ausbau-Standard wie zuletzt bei DSR/Löschkonzept/TOM/AVV anwenden — Fokusliste, URL-stabile Filter, Warnlogik, direkte Aufgaben-/PDCA-Folgeaktionen, Dashboard-Hinweise und anschließend wieder volle Release-Routine ab `1.13.1`.
- Bereits festgehaltener technischer Zwischenstand: Der erste Compile-/TypeScript-Check für den begonnenen KI-Compliance-Umbau lief grün; Audit sollte direkt im Anschluss nachgezogen werden. Konkrete Release-/Commitdaten für KI-Compliance oder Audit lagen zum Flush-Zeitpunkt noch nicht vor.
