# 2026-04-27

- PrivaShield auf Version 1.6.8 erhöht.
- Backup-/Restore-Problem für saubere Neuinstallationen analysiert und behoben.
- Neue Backups tragen jetzt Backend-Metadaten (lowdb/sqlite).
- Restore prüft Backend-Mismatch sauber und verhindert stille Fehlwiederherstellungen.
- Lowdb->SQLite-Wiederherstellung wurde zusätzlich als echte Migration umgesetzt.
- SQLite-Auto-Create in `server/db.ts` an das aktuelle Schema angeglichen, damit Restore-Migrationen nicht an veralteten Tabellenstrukturen scheitern.
- Passwort-Hashes, Benutzer-IDs und sicherheitsrelevante User-Felder werden bei lowdb->SQLite-Migration erhalten.
- UI für Backup/Restore zeigt jetzt das Backend des Backups und weist klarer auf Mismatch/Migration hin.
- Automatischer Test `server/backup.restore.test.ts` ergänzt, der lowdb->SQLite-Restore mit ID-/Hash-Erhalt absichert.
- Verifikation erfolgreich: `npm test` (30/30 Tests grün) und `npm run check` grün.
- Commit: `d33e28d` (`fix: harden backup restore migration for sqlite`).
- Push nach `master` erfolgreich; zusätzlicher Branch `1.6.8` erfolgreich auf GitHub angelegt/gepusht.
- Danach Laufzeitfehler entdeckt: `Cannot read properties of undefined (reading 'getAllUsers')` beim App-Start mit lowdb.
- Ursache war ein asynchroner Storage-Bootstrap in `server/storage.ts`, während Routen bereits synchron auf `storage` zugreifen.
- Hotfix umgesetzt: synchronen Storage-Bootstrap wiederhergestellt, damit lowdb-Start stabil bleibt.
- Hotfix verifiziert mit `npm run check` und `npm run build` erfolgreich.
- Hotfix-Commit: `b917701` (`fix: restore synchronous storage bootstrap`).
- Hotfix erfolgreich nach `master` gepusht; Branch `1.6.8` war bereits auf aktuellem Stand bzw. meldete kein zusätzliches Delta für separaten Push-Lauf.
- PDCA-/Audit-Workflow als nächster Funktionsblock ausgebaut und Version auf `1.7.0` erhöht.
- PDCA kann jetzt beim Anlegen optional direkt Folgeaufgaben erzeugen; die Übersicht zeigt dazu KPI und Workflow-Transparenz.
- Aufgaben spiegeln ihren Fortschritt und Status jetzt zurück in verknüpfte PDCA-Zyklen; PDCA-DO-Nachweise werden um eine Aufgabenübersicht ergänzt.
- Audit-Ansicht zeigt jetzt offene Folgeaufgaben, konkrete verknüpfte Maßnahmenaufgaben und stärkere Traceability über Audit → PDCA → Aufgabe.
- Beim Anlegen eines Audits kann nun optional automatisch ein Audit-Follow-up-PDCA erzeugt werden; Empfehlungen lassen sich zusätzlich direkt als Aufgaben vorbereiten.
- Verifikation für den Workflow-Block erfolgreich: `npm run check` und `npm run build` grün.
- Commit: `f0ba095` (`feat: connect audits with pdca follow-up workflows`).
- Push nach `master` erfolgreich; zusätzlicher Branch `1.7.0` erfolgreich auf GitHub angelegt/gepusht.
- Danach Export-/Druckkontext für Audit↔PDCA↔Aufgaben-Traceability nachgezogen und Version auf `1.7.1` erhöht.
- Exportkonfiguration übergibt jetzt PDCA-Folgeaufgaben explizit an die Druckansicht.
- Druckansicht zeigt im Audit-Block nun offene Folgeaufgaben sowie konkrete Maßnahmen aus PDCA-Follow-ups; der PDCA-Block enthält zusätzlich Maßnahmen-Traceability und offene Folgeaufgaben im Überblick.
- Verifikation erfolgreich: `npm run check` und `npm run build` grün.
- Commit: `4925e59` (`feat: extend audit pdca export traceability`).
- Push nach `master` erfolgreich; zusätzlicher Branch `1.7.1` erfolgreich auf GitHub angelegt/gepusht.
- Frontend-Crash beim Anlegen neuer PDCA-Zyklen behoben: kollidierende Schattenvariable im Audit/PDCA-Fallback entfernt.
- Verifikation erfolgreich: `npm run check` und `npm run build` grün.
- Commit: `ebcc80e` (`fix: resolve pdca creation frontend crash`).
- Fix erfolgreich nach `master` und Branch `1.7.1` gepusht.
