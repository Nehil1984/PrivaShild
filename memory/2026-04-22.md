# 2026-04-22

- PrivaShield-Login-/Mandantenproblem analysiert: Nach dem Login wurde der aktive Mandant in `client/src/App.tsx` nur aus `localStorage` geladen, aber nicht automatisch anhand der erlaubten Mandanten initialisiert.
- Fix umgesetzt: `AppRoutes` lädt nun nach vorhandenem Token die Mandantenliste und setzt automatisch den ersten erlaubten Mandanten, falls keine gültige Auswahl vorhanden ist. Dadurch ist die Mandantenauswahl direkt nach dem Login nutzbar und Seiten benötigen keinen Umweg über das Dashboard mehr.
- Verifikation erfolgreich: `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich durch.
- Wichtige dauerhafte Arbeitsregel für PrivaShield ergänzt: Vor jedem GitHub-Push immer diese Routine einhalten — (1) Funktionstest, (2) sicherstellen, dass keine lokalen Workspace-/Memory-/Identity-/OpenClaw-Dateien mitgepusht werden, (3) Push, (4) App-Stand danach im lokalen Workspace-Memory dokumentieren.
- Ebenfalls festgehalten: Workspace-/Memory-Dateien gehören nicht ins PrivaShield-GitHub-Repo, sondern bleiben lokal.
- Zusätzliche dauerhafte Vorgabe von Daniel: Bei jeder Überarbeitung der PrivaShield-App müssen die Versionsnummern-Regeln beachtet und die Version entsprechend korrekt fortgeschrieben werden.
- Neue Push-Regel von Daniel: Zusätzlich zum Push auf `master` soll künftig bei jedem Release-/Push-Schritt auch ein Branch mit der aktuellen Versionsnummer angelegt bzw. gepusht werden; `master` bleibt Hauptbranch.
- Security-Mittelupdate umgesetzt: Login wurde gehärtet mit benutzerbezogenen Sperrmechanismen. Nach wiederholten Fehlversuchen werden Fehlversuche gezählt, ab 5 Fehlversuchen greift eine temporäre Sperre, und ab insgesamt 15 Fehlversuchen wird der Benutzer administrativ gesperrt und muss durch einen Admin entsperrt werden.
- Benutzerobjekte und Storage um Sicherheitsfelder erweitert: Fehlversuche, Zeit-Sperre, Admin-Sperre und Zeitpunkte werden jetzt backendseitig mitgeführt.
- Admin-Entsperrung ergänzt: In der Benutzerverwaltung werden Sperrstatus sichtbar und gesperrte Benutzer können dort gezielt entsperrt werden.
- Mandantenzugriffsschutz zusätzlich testseitig abgesichert; Fokus bleibt, dass kein Benutzer auf nicht zugewiesene Mandantendaten zugreifen darf.
- Security-Verifikation erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Mittleres Update gemäß Versionsregel durchgeführt: Version von `1.2.17` auf `1.3.0` erhöht.
- Änderungen erfolgreich nach `master` gepusht. Commit: `7aaa9ac` `feat: haerte login und mandantenzugriff`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.3.0`.
- Login-UX für Security-Sperren verbessert: Das Frontend unterscheidet Sperrantworten jetzt klarer und zeigt bei temporären Sperren zusätzlich einen Hinweis, wann ungefähr ein neuer Versuch sinnvoll ist (`Retry-After` → Minutenhinweis).
- Patch-Version gemäß Versionsregel auf `1.3.1` erhöht.
- Verifikation erneut erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `e9db2fd` `fix: verbessere login-hinweise bei sperren`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.3.1`.
- Security-Audit der Routen/Admin-Funktionen fortgeführt und eine konkrete Härtung umgesetzt: Der Server akzeptiert bei `PUT /api/users/:id` jetzt nicht mehr ungefiltert beliebige Felder, sondern nur noch einen explizit erlaubten Satz. Die Entsperrlogik (`unlockUser`) wird dabei kontrolliert serverseitig in definierte Sicherheitsfeld-Resets übersetzt.
- Patch-Version gemäß Versionsregel auf `1.3.2` erhöht.
- Verifikation erneut erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `8717da8` `fix: haerte benutzer-updates serverseitig`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.3.2`.
- Zusätzliche Security-Negativtests ergänzt: Neue Testdatei sichert jetzt explizit ab, dass Benutzer-Updates nur einen erlaubten Feldsatz übernehmen, sicherheitsrelevante Lock-Felder nicht direkt über reguläre Updates manipulierbar sind und Sicherheits-Resets nur über den expliziten Entsperrpfad erfolgen.
- Patch-Version gemäß Versionsregel auf `1.3.3` erhöht.
- Verifikation erneut erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `10872d9` `test: sichere benutzer-update-regeln ab`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.3.3`.
- UX-/Hinweistext in der Benutzeranlage korrigiert: Der Passwort-Placeholder nennt jetzt nicht mehr fälschlich 8 Zeichen, sondern die tatsächliche Regel `mind. 12 Zeichen` inklusive Groß-/Kleinbuchstaben, Zahl und Sonderzeichen.
- Patch-Version gemäß Versionsregel auf `1.3.4` erhöht.
- Verifikation erneut erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `a24ba99` `fix: praezisiere passworthinweis in benutzeranlage`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.3.4`.
- Admin-Sicherheitsübersicht und Zugriffstransparenz ergänzt: Die Benutzerverwaltung zeigt jetzt kompakte Sicherheitskennzahlen (Admin-Sperren, Zeit-Sperren, Benutzer ohne Mandant, breite Zugriffe), eine zusammenfassende Sicherheitsübersicht sowie pro Benutzer die zugewiesenen Mandanten bzw. `Alle` für Administratoren.
- Funktionsupdate gemäß Versionsregel durchgeführt: Version von `1.3.4` auf `1.4.0` erhöht.
- Verifikation erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `d3620aa` `feat: ergaenze admin-sicherheitsuebersicht`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.4.0`.
- DSFA fachlich deutlich erweitert und auf `1.5.0` angehoben: Datenmodell um EDSA-nahe Strukturfelder ergänzt (u. a. Zweck, Prozessablauf, Verarbeitungskontext, Datenquellen, Empfänger, Drittlandtransfer, Auftragsverarbeiter, Technologien/Systeme, Profiling, automatisierte Entscheidung, strukturierte Verhältnismäßigkeitsbewertungen, Art.-36-/Freigabe-/DSB-/Review-Felder).
- DSFA-Formular in thematische Blöcke umgebaut: Grunddaten, Beschreibung der Verarbeitung, Notwendigkeit & Verhältnismäßigkeit, Risikoanalyse, Maßnahmen/Eskalation sowie DSB/Freigabe/Review.
- Risikoanalyse auf strukturierte Mehrfach-Risiken umgestellt; VVT-Verknüpfung mit Vorbefüllung zentraler Felder ergänzt.
- DSFA-Listenansicht erweitert um Warnindikatoren wie `Ohne VVT`, `Art. 36 prüfen`, `Review fällig` und `Hohes Restrisiko`.
- Zusätzliche Testabdeckung für defensive Normalisierung strukturierter DSFA-Risiken ergänzt.
- Verifikation erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `e1c59f7` `feat: erweitere dsfa struktur und workflow`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.5.0`.
- DSFA-Export/Druck an die neue 1.5.0-Struktur angepasst und als Patch auf `1.5.1` veröffentlicht.
- `client/print.html` erweitert: DSFA-Druckausgabe enthält jetzt deutlich mehr Fachfelder (u. a. VVT-Bezug, Zweck, Rechtsgrundlage, Verantwortlicher Bereich, Review) und gibt strukturierte Risikoobjekte als eigenen Risikoanalyse-Block aus.
- `client/src/export-page.tsx` aktualisiert: DSFA-Vorschau zeigt die neuen Kernfelder bereits vor dem Druck an.
- Verifikation erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `36fdf94` `fix: erweitere dsfa export fuer neue struktur`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.5.1`.
- DSFA-Serverhärtung umgesetzt und als Patch auf `1.5.2` veröffentlicht.
- `server/routes.ts` für DSFA aus dem generischen CRUD herausgezogen, damit strukturierte Risikoobjekte serverseitig normalisiert und validiert werden können.
- Neue Domänenregel für DSFA-Risiken: Risiken müssen als JSON-Liste kommen, jedes Risiko muss ein Objekt sein und mindestens einen Titel besitzen; serverseitig werden die Felder bereinigt und als konsistenter JSON-String gespeichert.
- Neuer Test `server/dsfa-validation.test.ts` ergänzt; Verifikation erfolgreich mit `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build`.
- Änderungen erfolgreich nach `master` gepusht. Commit: `6644adf` `fix: haerte dsfa risiko validierung serverseitig`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.5.2`.
- DSFA-Dashboard-/Warnlogik erweitert und als Patch auf `1.5.3` veröffentlicht.
- Dashboard berücksichtigt jetzt zusätzliche DSFA-Signale: `DSFA ohne VVT-Bezug`, `DSFA mit Art.-36-Prüfbedarf`, `überfällige DSFA-Reviews` und `DSFA mit hohem Restrisiko`.
- Die neuen DSFA-Kennzahlen fließen sowohl in die Hinweiskarte als auch in den Reifegrad-/Governance-Kontext ein.
- Verifikation erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `a806fe5` `feat: erweitere dsfa dashboard warnlogik`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.5.3`.
- DSFA-Fokusliste im Dashboard ergänzt und als Patch auf `1.5.4` veröffentlicht.
- Neue Dashboard-Karte `DSFA-Fokusliste` zeigt jetzt konkrete priorisierte Fälle statt nur Summen, u. a. für Art.-36-Prüfbedarf, hohe Restrisiken, überfällige Reviews und DSFA ohne VVT-Bezug.
- Damit werden aus DSFA-Warnsignalen jetzt direkte operative Handlungspunkte im Dashboard.
- Verifikation erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `ed8a1b3` `feat: ergaenze dsfa fokusliste im dashboard`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.5.4`.
- VVT↔DSFA-Verzahnung in der VVT-Liste verbessert und als Patch auf `1.5.5` veröffentlicht.
- Die VVT-Ansicht zeigt jetzt sichtbar, ob eine DSFA verknüpft ist, welche DSFA verknüpft ist und ob bei gesetztem `DSFA erforderlich` noch keine verknüpfte DSFA existiert.
- Neue Statushinweise in der VVT-Liste: `DSFA verknüpft` und `DSFA fehlt`.
- Verifikation erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `e4e2b93` `feat: verbessere vvt dsfa verknuepfung`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.5.5`.
- VVT-Quick-Check ergänzt und als Patch auf `1.5.6` veröffentlicht.
- Die VVT-Seite zeigt jetzt oberhalb der Liste einen kompakten Prüfblock für drei typische Lücken: `DSFA erforderlich, aber nicht verknüpft`, `VVT mit Drittlandtransfer` und `VVT ohne Löschkonzept-Bezug`.
- Damit ist die VVT-Seite nicht mehr nur Bestandsliste, sondern ein operativer Schnellcheck für Prioritäten im Verzeichnis der Verarbeitungstätigkeiten.
- Verifikation erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `95605be` `feat: ergaenze vvt quick check`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.5.6`.
- VVT-Fokusliste ergänzt und als Patch auf `1.5.7` veröffentlicht.
- Die VVT-Seite zeigt jetzt neben dem Quick-Check auch konkrete priorisierte Verarbeitungstätigkeiten, etwa bei fehlender DSFA-Verknüpfung, Drittlandtransfer oder fehlendem Löschkonzept-Bezug.
- Damit werden aus den bisherigen VVT-Summen jetzt direkte operative Handlungsfälle, analog zur DSFA-Fokusliste.
- Verifikation erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `d67e2d9` `feat: ergaenze vvt fokusliste`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.5.7`.
- Fokuslisten mit direkten CTAs/Filtern erweitert und als Patch auf `1.5.8` veröffentlicht.
- Die DSFA-Fokusliste enthält jetzt direkte Sprunglinks auf relevante Arbeitsseiten. Die VVT-Fokusliste kann problematische Fälle direkt in gefilterte Listenansichten überführen (`DSFA fehlt`, `Drittlandtransfer`, `Ohne Löschkonzept`).
- Damit wurden die bisherigen Fokuslisten von reinen Hinweislisten zu konkreteren Arbeitswerkzeugen weiterentwickelt.
- Verifikation erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `0cc06c2` `feat: ergaenze fokusfilter und ctas`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.5.8`.
- Fokusfilter robuster im Routing verankert und als Patch auf `1.5.9` veröffentlicht.
- DSFA- und VVT-Filterzustände werden jetzt per Query-Parameter getragen statt nur lokalem Zustand. Dadurch öffnen Fokus-Links die Zielseiten direkt im passenden Problemkontext (`/dsfa?filter=...`, `/vvt?filter=...`).
- Die bestehenden Fokuslisten/CTAs sind damit konsistenter, teilbarer und näher an einem echten Arbeitsworkflow.
- Verifikation erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `e4f5666` `feat: verankere fokusfilter im routing`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.5.9`.
- Sichtbare DSFA-Filterleiste ergänzt und als Patch auf `1.5.10` veröffentlicht.
- Die DSFA-Seite bietet jetzt – analog zur VVT-Seite – eine explizite Filter-UI für `Alle`, `Ohne VVT`, `Art. 36`, `Review fällig` und `Hohes Restrisiko`.
- Damit ist der per Routing eingeführte DSFA-Filterzustand nun auch direkt in der Oberfläche erkennbar und steuerbar.
- Verifikation erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `461c549` `feat: ergaenze sichtbare dsfa filterleiste`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.5.10`.
- Bugfix-Release `1.5.11` umgesetzt, um DSFA-/VVT-Filterzustände robuster zu machen.
- Die Filterauswertung in `DsfaPage` und `VvtPage` wurde von fragiler `split("?")`-Logik auf sauberes URL-Parsing umgestellt.
- Unbekannte oder alte Filterwerte werden nun defensiv auf `all` zurückgesetzt, statt Listen unbeabsichtigt leerzufiltern.
- Damit sollten gespeicherte DSFA wieder korrekt in der Liste erscheinen und die Auswahl-/Filterzustände in DSFA und VVT wieder zuverlässig umschaltbar sein.
- Verifikation erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `d9575a6` `fix: stabilisiere dsfa und vvt filterzustand`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.5.11`.
- Funktionsausbau `1.5.12` umgesetzt: echte Sortierung für DSFA und VVT sowie auditfreundlichere Druckstruktur für beide Module.
- `VvtPage` unterstützt jetzt Sortierungen nach `Name A–Z`, `Name Z–A`, `Status` und `Drittland zuerst`; `DsfaPage` unterstützt `Titel A–Z`, `Titel Z–A`, `Review zuerst` und `Hohes Risiko zuerst`.
- Filter- und Sortierzustände werden gemeinsam über die URL getragen, damit die Ansichten konsistent und reproduzierbar bleiben.
- `client/print.html` wurde für VVT und DSFA deutlich gegliedert: VVT mit Blöcken `Kontext & Steuerung` sowie `Datenbild & Beteiligte`; DSFA mit getrennten Blöcken für `Verarbeitungskontext`, `Bewertung, Maßnahmen & Governance` und Risikoanalyse.
- Verifikation erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `06e4c9d` `feat: erweitere sortierung und druckstruktur`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.5.12`.
- Weiterer Bugfix `1.5.13` umgesetzt, nachdem DSFA-/VVT-Listensteuerung trotz vorheriger Reparatur weiterhin fehlerhaft gemeldet wurde.
- Filter- und Sortierlogik in `DsfaPage` und `VvtPage` wurde von reinem URL-Live-State entkoppelt und nutzt jetzt lokalen React-State mit URL-Synchronisierung per `useEffect`.
- Ziel: Klicks auf Filter-/Sortierbuttons wirken sofort und robust, ohne dass Hash-/Routing-Details die Interaktion blockieren oder Listen unbeabsichtigt leer bleiben.
- Verifikation erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `b419843` `fix: entkopple listensteuerung von url state`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.5.13`.
- Transparenz-Fix `1.5.14` umgesetzt, um Mandantenkontext in DSFA- und VVT-Listen klar sichtbar zu machen.
- Beide Seiten zeigen jetzt oberhalb der Listen den aktiven Mandanten an und formulieren leere Zustände explizit mandantenbezogen.
- Ziel: sofort erkennbar machen, dass die jeweilige Liste nur Einträge des aktuell ausgewählten Mandanten zeigt und leere Zustände nicht mit Datenverlust verwechselt werden.
- Verifikation erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `24593d7` `fix: zeige mandantenkontext in listen`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.5.14`.
- Der eigentliche DSFA-Listenbug wurde in `1.5.15` behoben: Für die Listenansicht fehlte serverseitig die mandantenbezogene GET-Route `/api/mandanten/:mid/dsfa`.
- Export und Datenspeicherung funktionierten korrekt, weil der Export direkt `storage.getDsfaByMandant(mandantId)` nutzte; die DSFA-Seite griff dagegen ins Leere bzw. auf keinen passenden mandantenbezogenen Endpoint zu.
- Neue Route ergänzt: `GET /api/mandanten/:mid/dsfa` mit Mandantenzugriffsprüfung und sortierter Rückgabe nach `updatedAt`/`createdAt`.
- Verifikation erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `1104f33` `fix: ergaenze dsfa listenroute pro mandant`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.5.15`.
- API-/Smoke-Absicherung in `1.5.16` ergänzt: neuer Contract-Test für kritische Frontend-Module und Mandantenrouten.
- Ziel des Tests: fehlende mandantenbezogene Listenrouten oder zentrale Verwaltungsendpunkte künftig früh im Testlauf auffangen, statt erst über UI-Bugs.
- Der erste Testentwurf war zu naiv und schlug bei `aufgaben` fehl, weil die Route indirekt über `crudRoutes(...)` definiert wird; der Test wurde daraufhin auf explizite Routen plus CRUD-Moduldefinitionen korrigiert.
- Verifikation erfolgreich: `npm run test`, `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `ad243cd` `test: sichere api vertrag fuer kritische module`.
- Zusätzlich Versions-Branch erstellt und gepusht: `1.5.16`.
- Mandantenauswahl nach Login erneut nachgeschärft: Die Sidebar verwendet jetzt denselben erlaubten Mandanten-Satz wie die Initialisierung, deaktiviert die Auswahl während des Ladens sauber und zeigt nur wirklich sichtbare/erlaubte Mandanten an. Dadurch ist die Mandantenauswahl direkt nach dem Login konsistenter nutzbar.
- Versionsnummer gemäß Patch-Regel fortgeschrieben auf `1.2.15`.
- Verifikation vor Push erneut erfolgreich: `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `e93b0ab` `fix: verbessere mandantenauswahl nach login`.
- Login-/Startstrecke weiter gehärtet: Während der Mandanteninitialisierung zeigt die App jetzt einen globalen Vorbereitungszustand statt in leere Guard-Zustände zu fallen. Zusätzlich zeigt `MandantGuard` während des Mandanten-Ladens einen expliziten Ladezustand.
- Session-Restore bereinigt jetzt bei ungültigem Token nicht nur das Token, sondern auch die gespeicherte `privashield_active_mandant_id`, damit kein veralteter Mandantenkontext stehen bleibt.
- Versionsnummer gemäß Patch-Regel fortgeschrieben auf `1.2.16`.
- Verifikation vor Push erfolgreich: `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `c554529` `fix: stabilisiere login und mandantenkontext`.
- Einstiegs-Grenzfälle für Mandanten weiter verbessert: Benutzer ohne freigegebenen Mandanten erhalten jetzt einen klaren Hinweis statt eines unklaren Leerzustands. Bei genau einem sichtbaren Mandanten wird dieser Zustand explizit als einzige verfügbare Auswahl behandelt.
- Die Mandantenauswahl in der Sidebar ist jetzt bei 0 oder 1 sichtbaren Mandanten deaktiviert und zeigt passende Platzhaltertexte (`Kein Mandant verfügbar` bzw. direkt den einzigen Mandantennamen).
- Die automatische Mandanteninitialisierung berücksichtigt jetzt explizit den Ladezustand und behandelt den Ein-Mandant-Fall stabiler.
- Versionsnummer gemäß Patch-Regel fortgeschrieben auf `1.2.17`.
- Verifikation vor Push erfolgreich: `npm run check` und `JWT_SECRET=testsecret npm run build` liefen erfolgreich.
- Änderungen erfolgreich nach `master` gepusht. Commit: `d84ca40` `fix: verbessere mandanten-grenzfaelle im einstieg`.

- Wichtige Reihenfolge/Release-Historie für den heutigen PrivaShield-Stand konsolidiert: Nach dem Sicherheits-/Benutzerverwaltungsupdate auf `1.4.0` wurde die DSFA-/VVT-Ausbaulinie in zusammenhängenden Patch-Schritten bis `1.5.7` fortgeführt.
- Inhaltliche Linie `1.5.x`: `1.5.0` DSFA-Struktur & Workflow, `1.5.1` DSFA-Export/Druck, `1.5.2` serverseitige DSFA-Risikovalidierung, `1.5.3` DSFA-Dashboard-Warnlogik, `1.5.4` DSFA-Fokusliste im Dashboard, `1.5.5` sichtbare VVT↔DSFA-Verknüpfung in der VVT-Liste, `1.5.6` VVT-Quick-Check, `1.5.7` VVT-Fokusliste.
- Wichtige Produktentscheidung für PrivaShield bestätigt: Kein separates neues Security-/DSFA-Untermodul, sondern Ausbau der bestehenden Hauptseiten (`BenutzerPage`, `DsfaPage`, `VvtPage`, Dashboard, Export/Druck), damit operativer Mehrwert direkt im vorhandenen Workflow sichtbar wird.
- Technische Lehre aus dem heutigen Ausbau: Bei größeren `App.tsx`-Umbauten traten mehrfach JSX-/Objektliteral-Klammer- und Dublettenfehler auf; deshalb Änderungen an großen Komponenten künftig in kleinen, eindeutig abgegrenzten Blöcken lesen/ändern und `npm run check` früh laufen lassen.
- Fachliche Lehre für den DSFA-Bereich: EDSA-/EDPB-nahe DSFA-Unterstützung braucht zwingend vier Ebenen zusammen: strukturiertes Datenmodell, serverseitige Validierung der Risikoobjekte, operative Dashboard-/Fokuslogik und brauchbaren Export. Einzelne UI-Felder ohne diese Kette reichen nicht.
- Aktuell sinnvollster nächster Produkt-Schritt nach `1.5.7`: VVT-/DSFA-Fokusfälle mit direkten CTAs oder Filtern weiter operationalisieren (z. B. `DSFA anlegen`, `Löschkonzept ergänzen`) oder alternativ Export/Druck weiter auditfreundlich gliedern.

## 2026-04-22 – Abschluss DSFA-Listenfix & API-Absicherung

- Ursache für den gemeldeten DSFA-Fehler identifiziert: Die Listenansicht lud DSFA über `GET /api/mandanten/:mid/dsfa`, aber diese Route fehlte in `server/routes.ts`, während Export und Speicherung bereits funktionierten. Dadurch konnten DSFA im Export sichtbar sein, aber in der Seite leer bleiben.
- Fix umgesetzt in `server/routes.ts`: fehlende Route `GET /api/mandanten/:mid/dsfa` ergänzt, inklusive `requireMandantAccess(...)` und sortierter Rückgabe nach `updatedAt`/`createdAt` absteigend.
- Release `1.5.15` erstellt und veröffentlicht.
  - Commit: `1104f33`
  - Nachricht: `fix: ergaenze dsfa listenroute pro mandant`
  - Push nach `master` durchgeführt
  - zusätzlicher Versions-Branch `1.5.15` erstellt und gepusht
- Verifikation für `1.5.15` erfolgreich durchgeführt:
  - `npm run test`
  - `npm run check`
  - `JWT_SECRET=testsecret npm run build`
- Danach App-weiter API-Abgleich durchgeführt: Frontend-Aufrufe aus `client/src/App.tsx`, `client/src/export-page.tsx`, `client/print.html` und `client/src/hooks/useComplianceMeta.ts` mit `server/routes.ts` verglichen. Ergebnis: keine weitere offensichtliche fehlende API-Schnittstelle gefunden.
- Zusätzliche dauerhafte Absicherung ergänzt: neuer Contract-/Smoke-Test `server/api-contract.test.ts`, damit fehlende kritische Mandanten-Listenrouten künftig automatisiert auffallen.
- Wichtige Test-Erkenntnis: eine naive Stringsuche auf konkrete Routensegmente kann bei generisch verdrahteten `crudRoutes(...)` zu False Positives/False Negatives führen. Testlogik daher pragmatisch und routespezifisch halten, statt blind auf Stringlisten zu vertrauen.
- Release `1.5.16` erstellt und veröffentlicht.
  - Commit: `ad243cd`
  - Nachricht: `test: sichere api vertrag fuer kritische module`
  - Push nach `master` durchgeführt
  - zusätzlicher Versions-Branch `1.5.16` erstellt und gepusht
- Verifikation für `1.5.16` erfolgreich abgeschlossen. Erstlauf des neuen Tests schlug zunächst fehl (Testdesignproblem, kein Produktivbug), danach Test angepasst und Suite erfolgreich grün:
  - `npm run test`
  - `npm run check`
  - `JWT_SECRET=testsecret npm run build`
- Nutzerrückmeldung: Nach `1.5.15` funktioniert die DSFA-Anzeige jetzt wieder.
- Neuer Folgefehler bei vorhandenen DSFA-Einträgen identifiziert: Bearbeiten/Speichern schlug fehl, weil das Frontend per `useModuleData("dsfa")` ein `PUT /api/dsfa/:id` sendet, das Backend für DSFA-Updates aber nur `PATCH /api/dsfa/:id` bereitgestellt hatte.
- Fix umgesetzt in `server/routes.ts`: gemeinsamer DSFA-Update-Handler eingeführt und sowohl auf `PUT /api/dsfa/:id` als auch `PATCH /api/dsfa/:id` gelegt, jeweils mit bestehender Validierung über `insertDsfaSchema.partial()` und `sanitizeDsfaPayload(...)`.
- Contract-Test erweitert: `server/api-contract.test.ts` prüft jetzt zusätzlich, dass DSFA-Updates sowohl per PUT als auch per PATCH verfügbar sind.
- Release `1.5.17` erstellt und veröffentlicht.
  - Commit: `cee72ee`
  - Nachricht: `fix: erlaube dsfa updates per put und patch`
  - Push nach `master` durchgeführt
  - zusätzlicher Versions-Branch `1.5.17` erstellt und gepusht
- Verifikation für `1.5.17` erfolgreich durchgeführt:
  - `npm run test`
  - `npm run check`
  - `JWT_SECRET=testsecret npm run build`
