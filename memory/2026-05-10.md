# 2026-05-10

- PrivaShield weitergeführt und Security-/Auth-Cookie-Härtung abgeschlossen.
- Problemfokus: robuste Entscheidung, wann Auth-/CSRF-Cookies mit `Secure` gesetzt werden, damit HTTPS-/Proxy-Deployments stabil funktionieren, ohne lokale HTTP-Entwicklung auf `localhost`/`127.0.0.1` zu brechen.
- Umsetzung in `app/PrivaShield_Platform/dsgvo-platform/server/routes.ts` und `server/security.ts`: `x-forwarded-proto` wird jetzt auch bei kommaseparierten Werten wie `https, http` korrekt normalisiert; zusätzlich wird `req.secure` berücksichtigt; für nicht-lokale Hosts in Production wird `Secure` ebenfalls erzwungen, auch wenn ein Proxyheader fehlt.
- Tests in `app/PrivaShield_Platform/dsgvo-platform/server/security.test.ts` erweitert: Absicherung für Secure-Cookie-Setzung bei Proxy-HTTPS sowie Nicht-Setzung auf lokalem HTTP.
- Versionsnummer in `app/PrivaShield_Platform/dsgvo-platform/package.json` von `1.16.10` auf `1.16.11` erhöht.
- Verifikation erfolgreich: `npm test`, `npm run check` und `JWT_SECRET=testsecretfortestingonly1234567890 npm run build` liefen grün.
- Commit und Push erfolgreich: `64c81ce` (`fix: harden secure cookie detection`) nach `master` gepusht.
- Zusätzlicher Versionsbranch `1.16.11` erstellt und nach GitHub gepusht.
